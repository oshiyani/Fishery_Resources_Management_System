Default Admin Login
On first setup, run the seed script to create a default admin account:

​bash node seedAdmin.js ​

This creates an admin user with credentials defined in seedAdmin.js. 
Important: Change this password immediately after first login — the app will force this automatically via the "Change Password" flow.
